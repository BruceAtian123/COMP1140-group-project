package comp1140.ass2;

import org.junit.Rule;
import org.junit.Test;
import org.junit.rules.Timeout;

import static org.junit.Assert.assertEquals;

/*public class HighScoreTest {
    @Rule
    public Timeout globalTimeout = Timeout.millis(2000);

    @Test
    public void testUtility() {
        HighScoreBoard highScoreBoard = new HighScoreBoard();
        highScoreBoard.addHighScore(58,"Sam");
        highScoreBoard.addHighScore(80,"Tom");
        highScoreBoard.addHighScore(60,"Scout");
        highScoreBoard.addHighScore(90,"Rondo");
    }

    @Test
    public void check(int score, String name, HighScoreBoard hsb) {
        String actual = hsb.toString();
        assertEquals("HighScoreBoard.getBloom(size=" + size + ", board=\"" + board + "\", space=" + space + ") returned incorrect result:", expected, result);
    }
}*/
